import { PrintView } from "@/components/PrintView";

export const metadata = {
  title: "周暐倫 Terry — 履歷 / Résumé",
  robots: { index: false, follow: false },
};

export default function PrintPage() {
  return <PrintView />;
}
